export async function niceFetch(
  url: RequestInfo,
  opts?: RequestInit
): Promise<any> {
  const response = await fetch(url, {
    credentials: "include",
    ...opts,
  });

  return await response.json();
}
